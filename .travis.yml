language: node_js
dist: bionic

node_js:
  - 10

addons:
  postgresql: "10"

matrix:
  include:
    - node_js: "10"
      addons:
        postgresql: "10"

install:
  - npm install

cache: npm

script:
  - npm run lint
  - npm run test
  - npm run build
